import { Theme, ThemeConfig, CanvasOptions } from '../../core/src/index.ts';
export interface RibbonConfig extends ThemeConfig {
    size?: number;
}
export declare class Ribbon implements Theme<RibbonConfig> {
    #private;
    constructor({ size }?: RibbonConfig, canvasOptions?: CanvasOptions);
    mount(el: HTMLCanvasElement): void;
    unmount(): void;
}
//# sourceMappingURL=ribbon.d.ts.map